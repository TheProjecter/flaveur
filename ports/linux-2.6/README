If you run pkgmk with MENUCONFIG variable set like this:

    MENUCONFIG=yes pkgmk

the port will run a make menuconfig step.

The package will contain the modified configuration in 
    
    boot/config-$version

You may want to replace the supplied config file with this new one or your changes will be lost on next build
